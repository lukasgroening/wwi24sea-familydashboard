name: Deploy to Server

on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: self-hosted

    steps:
      - name: Deploy
        run: |
          cd ~/dashboard
          git pull
          HOST=${{ secrets.SSH_HOST }} ./build.sh
